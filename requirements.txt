# Dipendenze Gestionale OEPAC.
# Versioni con limite massimo (major) per evitare aggiornamenti incompatibili
# mantenendo la compatibilita' con Python 3.9+ (vedi README).
# Per un ambiente perfettamente riproducibile, sostituire i range con == .

flask>=2.3.0,<4.0
pandas>=2.0.0,<3.0          # pandas 3.x richiede Python 3.11+: restiamo su 2.x
openpyxl>=3.1.0,<4.0
xlsxwriter>=3.1.0,<4.0
python-docx>=1.0.0,<2.0
pyinstaller>=6.0.0,<7.0     # solo per creare l'eseguibile (build_exe)
pillow>=10.0.0,<12.0        # icone / generazione assets
webauthn>=2.0.0,<3.0        # login con impronta (Windows Hello), opzionale
