# Dipendenze per lo sviluppo e i test (non necessarie per l'uso normale).
# Installazione:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<10.0
pytest-cov>=5.0,<7.0
ruff>=0.6,<1.0
